# Layout change

I want to make a layout change:

1. Remove the outer border
2. Have the top part (clipboard, normalized, status) have its own border
3. Have the color box on the right like a sidebar (below the top part)
4. On the left have two bordered boxes:
   - the editor
   - the formats
